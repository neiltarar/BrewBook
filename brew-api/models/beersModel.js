import db from "../db/db.js";

export const addBeer = async (beerName, notes, location) => {
	try {
		const date = new Date();
		console.log(date);
		// const result = await db(
		// 	"INSERT INTO beers (user_id)"
		// );
	} catch (error) {}
};
